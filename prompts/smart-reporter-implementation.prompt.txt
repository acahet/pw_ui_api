Task: Implement playwright-smart-reporter for local and CI usage in a Playwright TypeScript project.

Requirements:
1) Local setup
- Add playwright-smart-reporter to Playwright reporters in playwright.config.ts.
- Keep existing HTML reporter and list reporter.
- Configure smart reporter as:
  - outputFile: tests/report/playwright-report/smart-report.html
  - historyFile: tests/report/smart-reporter-history/test-history.json
  - maxHistoryRuns: 30
  - runId: process.env.GITHUB_RUN_ID
- Keep one combined report across all Playwright projects (do NOT isolate by projectName).

2) Package scripts
- Add script: test:ci -> npx playwright test
- Add script: report:smart -> npx playwright-smart-reporter-serve tests/report/playwright-report/smart-report.html

3) Ignore rules
- Ensure tests/report/smart-reporter-history/ is gitignored.

4) Reusable CI workflow update
- In reusable workflow, when both run-api-tests and run-ui-tests are true, run one combined command (npx playwright test or yarn test:ci) so smart report is merged, not overwritten.
- Keep project-only runs for single-suite execution.
- Add cache restore/save for smart reporter history file:
  - tests/report/smart-reporter-history/test-history.json
- Upload report artifacts with retention-days: 30.

5) Validation
- Confirm no YAML syntax errors.
- Confirm reporter output path matches artifact upload path.

Deliverables:
- Updated playwright.config.ts
- Updated package.json scripts
- Updated .gitignore
- Updated reusable workflow yml
- Short summary of what changed and why
