Task: Implement playwright-smart-reporter for local and CI usage in a Playwright TypeScript project.

STATUS: ✅ IMPLEMENTED

Implementation Summary:
1) ✅ playwright.config.ts - Smart reporter configured with:
   - outputFile: ./tests/report/playwright-report/smart-report.html
   - historyFile: ./tests/report/smart-reporter-history/test-history.json
   - maxHistoryRuns: 30
   - runId: process.env.GITHUB_RUN_ID
   - Custom branding: "Playwright UI & API Report"
   - Combined report across all projects (ui-tests + api-tests)

2) ✅ package.json - Scripts added:
   - test:ci -> npx playwright test
   - report:smart -> npx playwright-smart-reporter-serve tests/report/playwright-report/smart-report.html

3) ✅ .gitignore - Smart reporter history ignored:
   - /tests/report/smart-reporter-history/*.json (allows .gitkeep but ignores JSON files)

4) ✅ Directory structure - Created with .gitkeep:
   - tests/report/smart-reporter-history/.gitkeep ensures directory exists in CI

5) ⏳ CI workflow - Pending update to reusable workflow:
   - Add cache restore/save for test-history.json
   - Ensure combined test run when both API and UI tests are enabled
   - Upload report artifacts with retention-days: 30
   - Confirm reporter output path matches artifact upload path

Key Implementation Details:
- Tab indentation used (required by Biome formatter)
- Multi-line string formatting for long paths
- Directory structure preserved in git via .gitkeep
- History file excluded from git but directory tracked

Next Steps:
- Update reusable CI workflow (acahet/shared-workflows/.github/workflows/playwright.yml)
- Add history file caching in CI
- Validate artifact upload path matches smart-report.html location
