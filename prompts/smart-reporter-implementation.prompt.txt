Task: Implement playwright-smart-reporter for local and CI usage in a Playwright TypeScript project.

STATUS: ✅ IMPLEMENTED (with GitHub Pages deployment)

Implementation Summary:
1) ✅ playwright.config.ts - Smart reporter configured with:
   - outputFile: ./tests/report/playwright-report/smart-report.html
   - historyFile: ./tests/report/smart-reporter-history/test-history.json
   - maxHistoryRuns: 30
   - runId: process.env.GITHUB_RUN_ID
   - Custom branding: "Playwright UI & API Report"
   - Combined report across all projects (ui-tests + api-tests)

2) ✅ package.json - Scripts added:
   - test:ci -> npx playwright test
   - report:smart -> npx playwright-smart-reporter-serve tests/report/playwright-report/smart-report.html

3) ✅ .gitignore - Smart reporter history ignored:
   - /tests/report/smart-reporter-history/*.json (allows .gitkeep but ignores JSON files)

4) ✅ Directory structure - Created with .gitkeep:
   - tests/report/smart-reporter-history/.gitkeep ensures directory exists in CI

5) ✅ GitHub Pages Deployment - New workflow added:
   - .github/workflows/deploy-report.yml
   - Automatically deploys reports to GitHub Pages after successful test runs
   - Creates user-friendly report hub (report-index.html)
   - Accessible via: https://<username>.github.io/<repo-name>/

6) ✅ Report Hub - Landing page created:
   - tests/report/playwright-report/report-index.html
   - Beautiful landing page with links to both reports
   - Automatically renamed to index.html during deployment

7) ✅ Documentation - README.md updated:
   - Added comprehensive "Test Reports" section
   - Local report access instructions
   - GitHub Pages setup instructions
   - Direct URLs to both smart and standard reports

Accessing Reports:
- Local: yarn report (standard) or yarn report:smart (smart reporter)
- CI/GitHub Pages:
  - Report Hub: https://<username>.github.io/<repo-name>/
  - Smart Report: https://<username>.github.io/<repo-name>/smart-report.html
  - Standard Report: Direct link from hub page

Setup Required:
1. Enable GitHub Pages in repository settings (Settings → Pages → Source: GitHub Actions)
2. Reports auto-deploy after successful test runs on main branch
3. Can also manually trigger deployment via workflow dispatch

Next Steps (Optional):
- ⏳ Update reusable shared workflow to add history file caching
- ⏳ Increase artifact retention-days from 7 to 30 in shared workflow
- ⏳ Consider combining test runs in shared workflow when both API/UI enabled
