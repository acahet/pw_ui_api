name: Deploy Test Reports to GitHub Pages

on:
    workflow_run:
        workflows: ["UI Tests CI"]  # Make sure this matches your CI workflow name exactly
        types:
            - completed
    # Allow manual trigger
    workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
    contents: read
    pages: write
    id-token: write

# Allow only one concurrent deployment
concurrency:
    group: "pages"
    cancel-in-progress: false

jobs:
    deploy:
        # Only run on successful test runs or manual trigger
        if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' }}
        environment:
            name: github-pages
            url: ${{ steps.deployment.outputs.page_url }}
        runs-on: ubuntu-latest
        steps:
            - name: Download artifact
              uses: actions/download-artifact@v4
              with:
                  github-token: ${{ secrets.GITHUB_TOKEN }}
                  run-id: ${{ github.event.workflow_run.id || github.run_id }}
                  name: playwright-report
                  path: ./report

            - name: Prepare report structure (Make Smart Report Root)
              run: |
                  # Make smart-report.html the default page
                  if [ -f ./report/smart-report.html ]; then
                      cp ./report/smart-report.html ./report/index.html
                  fi
                  # Keep hub page accessible
                  if [ -f ./report/report-index.html ]; then
                      mv ./report/report-index.html ./report/hub.html
                  fi
                  # List files for debugging
                  ls -la ./report/

            - name: Setup Pages
              uses: actions/configure-pages@v5

            - name: Upload artifact for GitHub Pages
              uses: actions/upload-pages-artifact@v3
              with:
                  path: ./report

            - name: Deploy to GitHub Pages
              id: deployment
              uses: actions/deploy-pages@v4
